# FanCast AI v2 Frontend Environment Configuration
# Copy this file to .env and fill in your actual values
# NOTE: Sensitive API keys should be stored in n8n, not here!

# Supabase Configuration (Public keys only)
VITE_SUPABASE_URL=https://fancastai.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# n8n Workflow Configuration (Webhook URLs only)
VITE_N8N_WEBHOOK_URL=your_n8n_webhook_url
VITE_N8N_AI_SCRIPT_GENERATION_WEBHOOK=your_n8n_ai_script_generation_webhook
VITE_N8N_AI_ASSISTED_CREATION_WEBHOOK=https://learncastai.app.n8n.cloud/webhook/ai-assisted-creation
VITE_N8N_LINE_PREVIEW_WEBHOOK="https://learncastai.app.n8n.cloud/webhook/cartesia-line-preview"
VITE_N8N_SCRIPT_PROCESSING_WEBHOOK=your_n8n_script_processing_webhook
VITE_N8N_AUDIO_GENERATION_WEBHOOK=your_n8n_audio_generation_webhook
VITE_N8N_FULL_SCRIPT_TTS_WEBHOOK=https://learncastai.app.n8n.cloud/webhook/ai-assisted-creation

# Stripe Configuration (Public key only)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key

# Development Configuration
VITE_APP_ENV=development

# ========================================
# SENSITIVE KEYS - STORE IN N8N ONLY!
# ========================================
# These should NOT be in your frontend .env:
# - CARTESIA_API_KEY (n8n handles TTS)
# - AWS_ACCESS_KEY_ID (n8n handles S3)
# - AWS_SECRET_ACCESS_KEY (n8n handles S3)
# - STRIPE_SECRET_KEY (n8n handles payments)
# - OPENAI_API_KEY (n8n handles AI script generation)
# ========================================
